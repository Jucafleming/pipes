import { FiltroArrayPipeImpuroPipe } from './filtro-array-pipe-impuro.pipe';

describe('FiltroArrayPipeImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrayPipeImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
